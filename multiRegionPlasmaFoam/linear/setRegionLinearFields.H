  //  const fvMesh& linearMesh = linearRegions[i];

    volScalarField& phi = phiLinear[i];
    volScalarField& sigma = sigmaLinear[i];
    volVectorField& J = JLinear[i];

    volScalarField& mu = muLinear[i];
    volVectorField& Hc = HcLinear[i];
    volVectorField& A = ALinear[i];
    volVectorField& B = BLinear[i];

